<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:eris.UI.ViewModels"
             xmlns:conv="clr-namespace:eris.UI.Converters"
             xmlns:views="clr-namespace:eris.UI.Views"
             x:Class="eris.UI.Views.MainPage"
             x:DataType="vm:MainViewModel"
             Title="{Binding PageTitle}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{Binding ToggleConfigLabel}"
                     Command="{Binding ToggleConfigCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <conv:InvertBoolConverter x:Key="InvertBool" />
        <conv:BoolToColorConverter x:Key="AuthStatusColor"
                                   TrueColor="#16a34a"
                                   FalseColor="#64748b" />
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*" BackgroundColor="{StaticResource Background}">

        <!-- ════════════════════════════════════════════════════════════════ -->
        <!-- ══  TAB 1: GENERA REPORT  ═════════════════════════════════════ -->
        <!-- ════════════════════════════════════════════════════════════════ -->
        <ScrollView Grid.Row="1" IsVisible="{Binding IsGenerateTab}">
            <VerticalStackLayout Padding="20,20,20,32" Spacing="0">

                <!-- Banner configurazione mancante -->
                <Label IsVisible="{Binding IsConfigIncomplete}"
                       FontSize="16" FontAttributes="Bold"
                       TextColor="{StaticResource OnSurfaceMuted}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center"
                       Margin="0,0,0,16">
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="Configura la sorgente per generare il report  " />
                            <Span Text="&#x2197;" FontSize="18" />
                        </FormattedString>
                    </Label.FormattedText>
                </Label>

                <!-- Logo -->
                <Image Source="eris_logo.png"
                       HeightRequest="240"
                       HorizontalOptions="Center"
                       Margin="0,12,0,16" />

                <!-- Messaggio benvenuto (visibile solo se config incompleta) -->
                <VerticalStackLayout IsVisible="{Binding IsConfigIncomplete}" Spacing="8" Margin="0,0,0,24">
                    <Label Text="Benvenuto"
                           FontSize="34"
                           FontAttributes="Bold"
                           TextColor="{StaticResource OnBackground}"
                           HorizontalTextAlignment="Center"
                           HorizontalOptions="Center"
                           Margin="0,12,0,12" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.2*" />
                            <ColumnDefinition Width="0.6*" />
                            <ColumnDefinition Width="0.2*" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="1"
                               Text="Eris è qui per estrarre l'essenza dalle tue giornate affollate. Trasforma la confusione del calendario in report ordinati: perché anche dalla discordia degli impegni può nascere un'armonia perfetta."
                               FontSize="18"
                               TextColor="{StaticResource OnSurfaceMuted}"
                               HorizontalTextAlignment="Center"
                               LineBreakMode="WordWrap" />
                    </Grid>
                </VerticalStackLayout>

                <!-- Contenuto principale (nascosto se config incompleta) -->
                <VerticalStackLayout Spacing="0"
                                     IsVisible="{Binding IsConfigIncomplete, Converter={StaticResource InvertBool}}">

                <!-- Intestazione -->
                <Label Text="Esporta i tuoi meeting in un report"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="{StaticResource OnBackground}"
                       HorizontalTextAlignment="Center"
                       HorizontalOptions="FillAndExpand"
                       IsVisible="{Binding ShowResult, Converter={StaticResource InvertBool}}"
                       Margin="0,12,0,24" />

                <Label Text="Ecco il tuo report"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="{StaticResource OnBackground}"
                       HorizontalTextAlignment="Center"
                       HorizontalOptions="FillAndExpand"
                       IsVisible="{Binding ShowResult}"
                       Margin="0,12,0,24" />

                <!-- ── Periodo (slider) ────────────────────────────────── -->
                <Border BackgroundColor="{StaticResource SurfaceVariant}"
                        StrokeThickness="0"
                        HeightRequest="46"
                        HorizontalOptions="FillAndExpand"
                        Margin="40,0,40,16"
                        IsVisible="{Binding ShowResult, Converter={StaticResource InvertBool}}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="23" />
                    </Border.StrokeShape>
                    <Grid ColumnDefinitions="*,*,*">
                        <Button Grid.Column="0"
                                Text="Questa Settimana"
                                Command="{Binding SelectThisWeekCommand}"
                                Margin="4" CornerRadius="19"
                                FontSize="13" Padding="0">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding IsThisWeekSelected}" Value="True">
                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                    <Setter Property="TextColor" Value="White" />
                                    <Setter Property="FontAttributes" Value="Bold" />
                                </DataTrigger>
                                <DataTrigger TargetType="Button" Binding="{Binding IsThisWeekSelected}" Value="False">
                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                    <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                    <Setter Property="FontAttributes" Value="None" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Grid.Column="1"
                                Text="Sett. Precedente"
                                Command="{Binding SelectCurrentMonthCommand}"
                                Margin="4" CornerRadius="19"
                                FontSize="13" Padding="0">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding IsCurrentMonthSelected}" Value="True">
                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                    <Setter Property="TextColor" Value="White" />
                                    <Setter Property="FontAttributes" Value="Bold" />
                                </DataTrigger>
                                <DataTrigger TargetType="Button" Binding="{Binding IsCurrentMonthSelected}" Value="False">
                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                    <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                    <Setter Property="FontAttributes" Value="None" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Grid.Column="2"
                                Text="Periodo Libero"
                                Command="{Binding SelectCustomPeriodCommand}"
                                Margin="4" CornerRadius="19"
                                FontSize="13" Padding="0">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding IsCustomPeriodSelected}" Value="True">
                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                    <Setter Property="TextColor" Value="White" />
                                    <Setter Property="FontAttributes" Value="Bold" />
                                </DataTrigger>
                                <DataTrigger TargetType="Button" Binding="{Binding IsCustomPeriodSelected}" Value="False">
                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                    <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                    <Setter Property="FontAttributes" Value="None" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                    </Grid>
                </Border>

                <!-- Periodo selezionato -->
                <Grid ColumnDefinitions="Auto,*,Auto"
                      ColumnSpacing="12"
                      HorizontalOptions="FillAndExpand"
                      Margin="40,4,40,20"
                      IsVisible="{Binding ShowResult, Converter={StaticResource InvertBool}}"
                      VerticalOptions="Center">
                    <Button Grid.Column="0"
                            Text="Lun – Ven"
                            FontSize="13" CornerRadius="19"
                            HeightRequest="46"
                            Padding="16,0" BorderWidth="1.5"
                            VerticalOptions="Center"
                            Command="{Binding ToggleWorkWeekCommand}">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button" Binding="{Binding IsWorkWeek}" Value="True">
                                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                <Setter Property="TextColor" Value="White" />
                                <Setter Property="BorderColor" Value="{StaticResource Primary}" />
                            </DataTrigger>
                            <DataTrigger TargetType="Button" Binding="{Binding IsWorkWeek}" Value="False">
                                <Setter Property="BackgroundColor" Value="Transparent" />
                                <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                <Setter Property="BorderColor" Value="{StaticResource SurfaceVariant}" />
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>

                    <Label Grid.Column="1"
                           Text="{Binding WeekRangeDisplay}"
                           FontSize="15"
                           FontAttributes="Bold"
                           TextColor="{StaticResource OnSurface}"
                           HorizontalTextAlignment="Center"
                           VerticalOptions="Center" />

                    <!-- Bottone calendario (periodo libero) -->
                    <Button Grid.Column="2"
                            Text="📅"
                            FontSize="18"
                            WidthRequest="36" HeightRequest="36"
                            CornerRadius="18"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource Primary}"
                            Padding="0"
                            VerticalOptions="Center"
                            IsVisible="{Binding IsCustomPeriodSelected}"
                            Command="{Binding OpenDatePickerCommand}" />
                </Grid>

                <!-- ── Pulsante Genera ────────────────────────────────────── -->
                <Grid HeightRequest="52"
                      IsVisible="{Binding ShowResult, Converter={StaticResource InvertBool}}"
                      Margin="40,0,40,12">
                    <Button Style="{StaticResource PrimaryBtn}"
                            FontSize="15"
                            HeightRequest="52"
                            Command="{Binding GenerateReportCommand}"
                            HorizontalOptions="FillAndExpand">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button" Binding="{Binding IsBusy}" Value="False">
                                <Setter Property="Text" Value="▶  Genera Report" />
                            </DataTrigger>
                            <DataTrigger TargetType="Button" Binding="{Binding IsBusy}" Value="True">
                                <Setter Property="Text" Value="" />
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                       IsVisible="{Binding IsBusy}"
                                       Color="White"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"
                                       HeightRequest="26"
                                       WidthRequest="26" />
                </Grid>

                  <!-- ── Errore ─────────────────────────────────────────────── -->
                  <Grid ColumnDefinitions="*,Auto"
                        ColumnSpacing="8"
                        IsVisible="{Binding HasError}"
                        Margin="0,0,0,12">
                      <Label Grid.Column="0"
                          Text="{Binding ErrorMessage}"
                          FontSize="17"
                          FontAttributes="Bold"
                          TextColor="{StaticResource Error}"
                          HorizontalTextAlignment="Center"
                          LineBreakMode="WordWrap"
                          VerticalOptions="Center" />
                      <Button Grid.Column="1"
                          Text="Copia"
                          Style="{StaticResource SecondaryBtn}"
                          FontSize="12"
                          HeightRequest="36"
                          Padding="10,0"
                          Command="{Binding CopyStatusCommand}" />
                  </Grid>

                <!-- ── Risultato: cerchi statistiche ──────────────────────────── -->
                <HorizontalStackLayout IsVisible="{Binding ShowResult}"
                                       HorizontalOptions="Center"
                                       Spacing="20"
                                       Margin="0,0,0,8">

                    <!-- Periodo -->
                    <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center"
                                         WidthRequest="150">
                        <Label Text="PERIODO"
                               FontSize="11" FontAttributes="Bold"
                               CharacterSpacing="1"
                               TextColor="{StaticResource OnSurfaceMuted}"
                               HorizontalOptions="Center" />
                        <Label Text="{Binding ResultPeriodStart}"
                               FontSize="20" FontAttributes="Bold"
                               TextColor="{StaticResource OnBackground}"
                               HorizontalOptions="Center" />
                        <Label Text="–"
                               FontSize="14"
                               TextColor="{StaticResource OnSurfaceMuted}"
                               HorizontalOptions="Center" />
                        <Label Text="{Binding ResultPeriodEnd}"
                               FontSize="20" FontAttributes="Bold"
                               TextColor="{StaticResource OnBackground}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>

                    <views:CircularStatView Icon="⏱"
                                            StatLabel="Ore"
                                            Value="{Binding TotalHours, StringFormat='{0:F1}h'}"
                                            Progress="{Binding HoursProgress}" />

                    <views:CircularStatView Icon="📊"
                                            StatLabel="Meeting"
                                            Value="{Binding MeetingCount, StringFormat='{0}'}"
                                            Progress="{Binding MeetingProgress}" />

                </HorizontalStackLayout>

                <!-- Bottoni post-risultato -->
                <VerticalStackLayout IsVisible="{Binding ShowResult}" Spacing="8" Margin="0,12,0,12">
                    <Button Text="Nuovo Report"
                            Style="{StaticResource SecondaryBtn}"
                            HeightRequest="52"
                            FontSize="15"
                            HorizontalOptions="FillAndExpand"
                            Command="{Binding NuovoReportCommand}" />
                    <Button Text="Apri Report"
                            Style="{StaticResource PrimaryBtn}"
                            HeightRequest="52"
                            FontSize="15"
                            HorizontalOptions="FillAndExpand"
                            Command="{Binding OpenResultCommand}" />
                </VerticalStackLayout>

                </VerticalStackLayout>
                <!-- fine: Contenuto principale -->

            </VerticalStackLayout>
        </ScrollView>

        <!-- ════════════════════════════════════════════════════════════════ -->
        <!-- ══  TAB 2: CONFIGURAZIONE  ════════════════════════════════════ -->
        <!-- ════════════════════════════════════════════════════════════════ -->
        <ScrollView Grid.Row="1" IsVisible="{Binding IsConfigTab}">
            <VerticalStackLayout Padding="20,32,20,32" Spacing="0">

                <!-- ── Sorgente Calendario ─────────────────────────── -->
                <Grid ColumnDefinitions="45*,20*,35*" ColumnSpacing="0" Margin="0,0,0,32" Padding="0">
                    <VerticalStackLayout Grid.Column="0" Spacing="4">
                        <Label Text="Scegli da dove leggere i tuoi eventi."
                               Style="{StaticResource MutedLabel}"
                               LineBreakMode="WordWrap" />

                        <Border BackgroundColor="{StaticResource SurfaceVariant}"
                                StrokeThickness="0"
                                HeightRequest="46">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="23" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="*,*">
                                <Button Grid.Column="0"
                                        Text="ICS Calendar"
                                        Command="{Binding SelectSourceIcsCommand}"
                                        Margin="4" CornerRadius="19"
                                        FontSize="14" Padding="0">
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Binding="{Binding IsIcsSource}" Value="True">
                                            <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                            <Setter Property="TextColor" Value="White" />
                                            <Setter Property="FontAttributes" Value="Bold" />
                                        </DataTrigger>
                                        <DataTrigger TargetType="Button" Binding="{Binding IsIcsSource}" Value="False">
                                            <Setter Property="BackgroundColor" Value="Transparent" />
                                            <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                            <Setter Property="FontAttributes" Value="None" />
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                                <Button Grid.Column="1"
                                        Text="Outlook"
                                        Command="{Binding SelectSourceGraphCommand}"
                                        Margin="4" CornerRadius="19"
                                        FontSize="14" Padding="0">
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Binding="{Binding IsGraphSource}" Value="True">
                                            <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                            <Setter Property="TextColor" Value="White" />
                                            <Setter Property="FontAttributes" Value="Bold" />
                                        </DataTrigger>
                                        <DataTrigger TargetType="Button" Binding="{Binding IsGraphSource}" Value="False">
                                            <Setter Property="BackgroundColor" Value="Transparent" />
                                            <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                            <Setter Property="FontAttributes" Value="None" />
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                            </Grid>
                        </Border>
                    </VerticalStackLayout>

                    <!-- Formato Subject -->
                    <VerticalStackLayout Grid.Column="2" Spacing="4">
                        <Label Text="Struttura dei titoli."
                               Style="{StaticResource MutedLabel}"
                               LineBreakMode="WordWrap" />
                        <Button Text="Formato Subject"
                                Style="{StaticResource SecondaryBtn}"
                                HeightRequest="46"
                                FontSize="13"
                                Padding="16,8"
                                Command="{Binding OpenTemplateDialogCommand}" />
                    </VerticalStackLayout>
                </Grid>

                <!-- ── URL ICS ─────────────────────────────────────── -->
                <VerticalStackLayout Spacing="10" Margin="0,0,0,32" Padding="0,4"
                                    IsVisible="{Binding IsIcsSource}">
                        <Label Text="Inserisci l'URL del file .ics del tuo calendario. Lo trovi nelle impostazioni del tuo provider di calendario."
                               Style="{StaticResource MutedLabel}"
                               LineBreakMode="WordWrap"
                               Margin="0,0,0,4" />
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8" VerticalOptions="Center">
                            <Entry Grid.Column="0"
                                   Text="{Binding IcsUrl}"
                                   TextColor="{StaticResource OnSurface}"
                                   BackgroundColor="{StaticResource SurfaceVariant}"
                                   PlaceholderColor="{StaticResource OnSurfaceMuted}"
                                   Placeholder="https://example.com/calendar.ics"
                                   Keyboard="Default"
                                   FontSize="13" />
                            <Button Grid.Column="1"
                                    Text="ⓘ"
                                    FontSize="20"
                                    WidthRequest="36" HeightRequest="36"
                                    CornerRadius="18"
                                    BackgroundColor="Transparent"
                                    TextColor="{StaticResource OnSurfaceMuted}"
                                    Padding="0"
                                    VerticalOptions="Center"
                                    Command="{Binding ShowIcsHelpCommand}" />
                        </Grid>
                </VerticalStackLayout>

                <!-- ── Account Microsoft ───────────────────────────── -->
                <VerticalStackLayout Spacing="10" Margin="0,0,0,32" Padding="0,4"
                                    IsVisible="{Binding IsGraphSource}">
                        <Label Text="Accedi con il tuo account Microsoft 365 per leggere gli eventi dal calendario Outlook."
                               Style="{StaticResource MutedLabel}"
                               LineBreakMode="WordWrap"
                               Margin="0,0,0,4" />

                        <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                            <Ellipse WidthRequest="10" HeightRequest="10" VerticalOptions="Center"
                                     Fill="{Binding IsAuthenticated, Converter={StaticResource AuthStatusColor}}" />
                            <Label Text="{Binding UserDisplayName}"
                                   Style="{StaticResource SubtitleLabel}"
                                   VerticalOptions="Center" />
                        </HorizontalStackLayout>

                        <Frame IsVisible="{Binding HasDeviceCode}"
                               BackgroundColor="#1c3448" BorderColor="#0284c7"
                               CornerRadius="8" Padding="12">
                            <VerticalStackLayout Spacing="6">
                                <Label Text="Autenticazione via codice dispositivo:"
                                       Style="{StaticResource MutedLabel}" />
                                <Label Text="{Binding DeviceCodeMessage}"
                                       TextColor="{StaticResource PrimaryLight}"
                                       FontSize="12" LineBreakMode="WordWrap" />
                            </VerticalStackLayout>
                        </Frame>

                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8"
                              IsVisible="{Binding IsBusy, Converter={StaticResource InvertBool}}">
                            <Button Grid.Column="0"
                                    Text="Accedi con Microsoft"
                                    Style="{StaticResource PrimaryBtn}"
                                    Command="{Binding LoginCommand}"
                                    IsVisible="{Binding IsNotAuthenticated}" />
                            <Button Grid.Column="0"
                                    Text="✓  Autenticato"
                                    Style="{StaticResource PrimaryBtn}"
                                    BackgroundColor="{StaticResource Success}"
                                    IsEnabled="False"
                                    IsVisible="{Binding IsAuthenticated}" />
                            <Button Grid.Column="1"
                                    Text="Esci"
                                    Style="{StaticResource DangerBtn}"
                                    Command="{Binding LogoutCommand}"
                                    IsVisible="{Binding IsAuthenticated}" />
                        </Grid>

                        <ActivityIndicator IsRunning="{Binding IsBusy}"
                                           IsVisible="{Binding IsBusy}"
                                           Color="{StaticResource Primary}"
                                           HeightRequest="32" />

                           <Grid ColumnDefinitions="*,Auto"
                              ColumnSpacing="8"
                              IsVisible="{Binding HasAuthError}"
                              Margin="0,2,0,0">
                            <Label Grid.Column="0"
                                Text="{Binding AuthErrorMessage}"
                                FontSize="14"
                                FontAttributes="Bold"
                                TextColor="{StaticResource Error}"
                                HorizontalTextAlignment="Start"
                                LineBreakMode="WordWrap"
                                VerticalOptions="Center" />
                            <Button Grid.Column="1"
                                 Text="Copia"
                                 Style="{StaticResource SecondaryBtn}"
                                 FontSize="12"
                                 HeightRequest="36"
                                 Padding="10,0"
                                 Command="{Binding CopyStatusCommand}" />
                           </Grid>
                </VerticalStackLayout>

                <BoxView HeightRequest="1"
                         BackgroundColor="{StaticResource SurfaceVariant}"
                         Margin="0,4,0,4" />

                <!-- ── Cartella di output ──────────────────────────── -->
                <VerticalStackLayout Spacing="10" Margin="0,0,0,32" Padding="0,4">
                        <Label Text="I file del report verranno salvati in questa cartella."
                               Style="{StaticResource MutedLabel}"
                               LineBreakMode="WordWrap"
                               Margin="0,0,0,4" />
                        <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                            <Border Grid.Column="0"
                                    BackgroundColor="{StaticResource SurfaceVariant}"
                                    StrokeThickness="0"
                                    Padding="12,0"
                                    HeightRequest="46">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8" />
                                </Border.StrokeShape>
                                <Label Text="{Binding OutputFolderName}"
                                       ToolTipProperties.Text="{Binding OutputFolder}"
                                       TextColor="{StaticResource OnSurface}"
                                       FontSize="15"
                                       VerticalOptions="Center"
                                       VerticalTextAlignment="Center"
                                       LineBreakMode="TailTruncation" />
                            </Border>
                            <Button Grid.Column="1"
                                    Text="Seleziona"
                                    Style="{StaticResource SecondaryBtn}"
                                    FontSize="13"
                                    Command="{Binding SelectFolderCommand}" />
                            <Button Grid.Column="2"
                                    Text="↗"
                                    Style="{StaticResource SecondaryBtn}"
                                    WidthRequest="44"
                                    Command="{Binding OpenOutputFolderCommand}" />
                        </Grid>
                </VerticalStackLayout>

                <!-- ── Formato esportazione + Ore settimanali ────────────── -->
                <VerticalStackLayout Spacing="10" Margin="0,0,0,32" Padding="0,4">
                        <Grid ColumnDefinitions="*,*,*" ColumnSpacing="14">
                            <!-- Formato -->
                            <VerticalStackLayout Grid.Column="0" Spacing="8">
                                <Label Text="Formato del file generato: Excel o CSV."
                                       Style="{StaticResource MutedLabel}"
                                       LineBreakMode="WordWrap"
                                        HeightRequest="36"
                                       Margin="0,0,0,4" />
                                <Border BackgroundColor="{StaticResource SurfaceVariant}"
                                        StrokeThickness="0"
                                        HeightRequest="46">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="23" />
                                    </Border.StrokeShape>
                                    <Grid ColumnDefinitions="*,*">
                                        <Button Grid.Column="0"
                                                Text="XLSX"
                                                Command="{Binding SelectFormatXlsxCommand}"
                                                Margin="4" CornerRadius="19"
                                                FontSize="14" Padding="0">
                                            <Button.Triggers>
                                                <DataTrigger TargetType="Button" Binding="{Binding IsXlsxFormat}" Value="True">
                                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                                    <Setter Property="TextColor" Value="White" />
                                                    <Setter Property="FontAttributes" Value="Bold" />
                                                </DataTrigger>
                                                <DataTrigger TargetType="Button" Binding="{Binding IsXlsxFormat}" Value="False">
                                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                                    <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                                    <Setter Property="FontAttributes" Value="None" />
                                                </DataTrigger>
                                            </Button.Triggers>
                                        </Button>
                                        <Button Grid.Column="1"
                                                Text="CSV"
                                                Command="{Binding SelectFormatCsvCommand}"
                                                Margin="4" CornerRadius="19"
                                                FontSize="14" Padding="0">
                                            <Button.Triggers>
                                                <DataTrigger TargetType="Button" Binding="{Binding IsCsvFormat}" Value="True">
                                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                                    <Setter Property="TextColor" Value="White" />
                                                    <Setter Property="FontAttributes" Value="Bold" />
                                                </DataTrigger>
                                                <DataTrigger TargetType="Button" Binding="{Binding IsCsvFormat}" Value="False">
                                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                                    <Setter Property="TextColor" Value="{StaticResource OnSurfaceMuted}" />
                                                    <Setter Property="FontAttributes" Value="None" />
                                                </DataTrigger>
                                            </Button.Triggers>
                                        </Button>
                                    </Grid>
                                </Border>
                            </VerticalStackLayout>
                            <!-- Ore settimanali -->
                            <VerticalStackLayout Grid.Column="1" Spacing="8">
                                <Label Text="Monte ore lavorative settimanali (es. 40)."
                                       Style="{StaticResource MutedLabel}"
                                       LineBreakMode="WordWrap"
                                        HeightRequest="36"
                                       Margin="0,0,0,4" />
                                <Entry Text="{Binding WeeklyWorkingHours}"
                                       TextColor="{StaticResource OnSurface}"
                                       BackgroundColor="{StaticResource SurfaceVariant}"
                                       PlaceholderColor="{StaticResource OnSurfaceMuted}"
                                       Placeholder="40"
                                       Keyboard="Numeric"
                                       HeightRequest="46"
                                       FontSize="15"
                                       HorizontalTextAlignment="Center" />
                            </VerticalStackLayout>

                            <!-- Filtri -->
                            <VerticalStackLayout Grid.Column="2" Spacing="8">
                                <Label Text="Filtri attività da escludere."
                                       Style="{StaticResource MutedLabel}"
                                       LineBreakMode="WordWrap"
                                        HeightRequest="36"
                                       Margin="0,0,0,4" />
                                <Grid>
                                    <Button Text="Filtri"
                                            Style="{StaticResource SecondaryBtn}"
                                          HeightRequest="46"
                                            FontSize="13"
                                            Padding="16,8"
                                            Command="{Binding OpenFiltersDialogCommand}" />

                                    <Border IsVisible="{Binding HasActiveFilters}"
                                            BackgroundColor="{StaticResource Primary}"
                                            StrokeThickness="0"
                                            WidthRequest="10"
                                            HeightRequest="10"
                                            HorizontalOptions="End"
                                            VerticalOptions="Start"
                                            Margin="0,4,6,0">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="5" />
                                        </Border.StrokeShape>
                                    </Border>
                                </Grid>
                            </VerticalStackLayout>
                        </Grid>
                </VerticalStackLayout>

                <!-- ── Chiudi ──────────────────────────────────────────── -->
                <Button Text="Chiudi e torna in home"
                        Style="{StaticResource SecondaryBtn}"
                        HeightRequest="52"
                        FontSize="15"
                        HorizontalOptions="FillAndExpand"
                        Margin="0,8,0,0"
                        Command="{Binding SelectTabGenerateCommand}" />

            </VerticalStackLayout>
        </ScrollView>

        <!-- ── Dialog selezione periodo ──────────────────────────────────── -->
        <Grid Grid.RowSpan="2" IsVisible="{Binding IsDatePickerOpen}"
              BackgroundColor="#CC000000">
            <Frame BackgroundColor="{StaticResource Surface}"
                   BorderColor="{StaticResource SurfaceVariant}"
                   CornerRadius="16" HasShadow="False"
                   Padding="28" WidthRequest="320"
                   HorizontalOptions="Center" VerticalOptions="Center">
                <VerticalStackLayout Spacing="16">

                    <Label Text="Seleziona Periodo"
                           FontSize="17" FontAttributes="Bold"
                           TextColor="{StaticResource OnBackground}"
                           HorizontalOptions="Center" />

                    <VerticalStackLayout Spacing="6">
                        <Label Text="DA" FontSize="11" FontAttributes="Bold"
                               TextColor="{StaticResource OnSurfaceMuted}" />
                        <DatePicker Date="{Binding DialogStartDate}"
                                    Format="dd/MM/yyyy"
                                    TextColor="{StaticResource OnSurface}"
                                    BackgroundColor="{StaticResource SurfaceVariant}"
                                    HorizontalOptions="FillAndExpand" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="6">
                        <Label Text="A" FontSize="11" FontAttributes="Bold"
                               TextColor="{StaticResource OnSurfaceMuted}" />
                        <DatePicker Date="{Binding DialogEndDate}"
                                    Format="dd/MM/yyyy"
                                    TextColor="{StaticResource OnSurface}"
                                    BackgroundColor="{StaticResource SurfaceVariant}"
                                    HorizontalOptions="FillAndExpand" />
                    </VerticalStackLayout>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,8,0,0">
                        <Button Grid.Column="0" Text="Annulla"
                                Style="{StaticResource SecondaryBtn}"
                                HeightRequest="44"
                                Command="{Binding CancelDatePickerCommand}" />
                        <Button Grid.Column="1" Text="Conferma"
                                Style="{StaticResource PrimaryBtn}"
                                HeightRequest="44"
                                Command="{Binding ConfirmDatePickerCommand}" />
                    </Grid>

                </VerticalStackLayout>
            </Frame>
        </Grid>

             <!-- ── Dialog filtri attività ─────────────────────────────────────── -->
             <Grid Grid.RowSpan="2" IsVisible="{Binding IsFiltersDialogOpen}"
                BackgroundColor="#CC000000">
                 <Frame BackgroundColor="{StaticResource Surface}"
                     BorderColor="{StaticResource SurfaceVariant}"
                     CornerRadius="16" HasShadow="False"
                     Padding="24" WidthRequest="360"
                     HorizontalOptions="Center" VerticalOptions="Center">
                  <VerticalStackLayout Spacing="12">

                      <Label Text="Filtri"
                          FontSize="18"
                          FontAttributes="Bold"
                          TextColor="{StaticResource OnBackground}"
                          HorizontalOptions="Center" />

                      <Label Text="Gli eventi corrispondenti verranno esclusi dal report. Separa i valori con una virgola."
                          Style="{StaticResource MutedLabel}"
                          HorizontalTextAlignment="Center" />

                      <!-- Escludi tentative -->
                      <Grid ColumnDefinitions="*,Auto" Margin="0,4,0,4">
                          <Label Grid.Column="0"
                              Text="Escludi eventi &quot;tentative&quot;"
                              TextColor="{StaticResource OnSurface}"
                              FontSize="13"
                              FontAttributes="Bold"
                              VerticalOptions="Center" />
                          <Switch Grid.Column="1"
                              IsToggled="{Binding DialogExcludeTentative}"
                              OnColor="{StaticResource Primary}"
                              VerticalOptions="Center" />
                      </Grid>

                      <Label Text="Categorie (tag)"
                          TextColor="{StaticResource OnSurface}"
                          FontSize="13"
                          FontAttributes="Bold" />
                      <Entry Text="{Binding DialogExcludedCategories}"
                          TextColor="{StaticResource OnSurface}"
                          BackgroundColor="{StaticResource SurfaceVariant}"
                          PlaceholderColor="{StaticResource OnSurfaceMuted}"
                          Placeholder="Personale, OOO"
                          Keyboard="Default"
                          FontSize="13" />

                      <Label Text="Cliente"
                          TextColor="{StaticResource OnSurface}"
                          FontSize="13"
                          FontAttributes="Bold"
                          Margin="0,4,0,0" />
                      <Entry Text="{Binding DialogExcludedClients}"
                          TextColor="{StaticResource OnSurface}"
                          BackgroundColor="{StaticResource SurfaceVariant}"
                          PlaceholderColor="{StaticResource OnSurfaceMuted}"
                          Placeholder="ClienteX, ClienteY"
                          Keyboard="Default"
                          FontSize="13" />

                      <Label Text="Progetto"
                          TextColor="{StaticResource OnSurface}"
                          FontSize="13"
                          FontAttributes="Bold"
                          Margin="0,4,0,0" />
                      <Entry Text="{Binding DialogExcludedProjects}"
                          TextColor="{StaticResource OnSurface}"
                          BackgroundColor="{StaticResource SurfaceVariant}"
                          PlaceholderColor="{StaticResource OnSurfaceMuted}"
                          Placeholder="ProgettoA, ProgettoB"
                          Keyboard="Default"
                          FontSize="13" />

                      <Label Text="Topic"
                          TextColor="{StaticResource OnSurface}"
                          FontSize="13"
                          FontAttributes="Bold"
                          Margin="0,4,0,0" />
                      <Entry Text="{Binding DialogExcludedTopics}"
                          TextColor="{StaticResource OnSurface}"
                          BackgroundColor="{StaticResource SurfaceVariant}"
                          PlaceholderColor="{StaticResource OnSurfaceMuted}"
                          Placeholder="TopicX, TopicY"
                          Keyboard="Default"
                          FontSize="13" />

                      <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,10,0,0">
                       <Button Grid.Column="0"
                            Text="Annulla"
                            Style="{StaticResource SecondaryBtn}"
                            HeightRequest="44"
                            Command="{Binding CancelFiltersDialogCommand}" />
                       <Button Grid.Column="1"
                            Text="Salva"
                            Style="{StaticResource PrimaryBtn}"
                            HeightRequest="44"
                            Command="{Binding ApplyFiltersDialogCommand}" />
                      </Grid>

                  </VerticalStackLayout>
                 </Frame>
             </Grid>

             <!-- ── Dialog formato subject ─────────────────────────────────────── -->
             <Grid Grid.RowSpan="2" IsVisible="{Binding IsTemplateDialogOpen}"
                BackgroundColor="#CC000000">
                 <Frame BackgroundColor="{StaticResource Surface}"
                     BorderColor="{StaticResource SurfaceVariant}"
                     CornerRadius="16" HasShadow="False"
                     Padding="24" WidthRequest="420"
                     HorizontalOptions="Center" VerticalOptions="Center">
                  <VerticalStackLayout Spacing="12">

                      <Label Text="Formato Subject"
                          FontSize="18"
                          FontAttributes="Bold"
                          TextColor="{StaticResource OnBackground}"
                          HorizontalOptions="Center" />

                      <Label Text="Definisci come sono strutturati i titoli dei tuoi eventi nel calendario. Usa i placeholder &#x7B;Cliente&#x7D;, &#x7B;Progetto&#x7D; e &#x7B;Topic&#x7D; separati dal carattere che usi nei titoli."
                          Style="{StaticResource MutedLabel}"
                          HorizontalTextAlignment="Center"
                          LineBreakMode="WordWrap" />

                      <Label Text="Esempio: se i tuoi eventi si chiamano &quot;Acme | Website | Review&quot;, il template è:"
                          Style="{StaticResource MutedLabel}"
                          HorizontalTextAlignment="Center"
                          LineBreakMode="WordWrap" />

                      <Label Text="{}&#x7B;Cliente&#x7D; | &#x7B;Progetto&#x7D; | &#x7B;Topic&#x7D;"
                          FontSize="14"
                          FontAttributes="Bold"
                          TextColor="{StaticResource Primary}"
                          HorizontalTextAlignment="Center" />

                      <Label Text="Template"
                          TextColor="{StaticResource OnSurface}"
                          FontSize="13"
                          FontAttributes="Bold"
                          Margin="0,4,0,0" />
                      <Entry Text="{Binding DialogSubjectTemplate}"
                          TextColor="{StaticResource OnSurface}"
                          BackgroundColor="{StaticResource SurfaceVariant}"
                          PlaceholderColor="{StaticResource OnSurfaceMuted}"
                          Placeholder="{}&#x7B;Cliente&#x7D; | &#x7B;Progetto&#x7D; | &#x7B;Topic&#x7D;"
                          Keyboard="Default"
                          FontSize="13" />

                      <Button Text="Ripristina Default"
                          Style="{StaticResource SecondaryBtn}"
                          HeightRequest="36"
                          FontSize="12"
                          Padding="10,0"
                          HorizontalOptions="End"
                          Command="{Binding ResetTemplateDialogCommand}" />

                      <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,6,0,0">
                       <Button Grid.Column="0"
                            Text="Annulla"
                            Style="{StaticResource SecondaryBtn}"
                            HeightRequest="44"
                            Command="{Binding CancelTemplateDialogCommand}" />
                       <Button Grid.Column="1"
                            Text="Salva"
                            Style="{StaticResource PrimaryBtn}"
                            HeightRequest="44"
                            Command="{Binding ApplyTemplateDialogCommand}" />
                      </Grid>

                  </VerticalStackLayout>
                 </Frame>
             </Grid>

    </Grid>
</ContentPage>
